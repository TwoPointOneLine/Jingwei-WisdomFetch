# 本 .dockerignore 针对 build context = ../backend（见 deploy/compose.yml）
# 仅列出 backend 上下文内真正需要排除的项；根级 frontend/docs/logs 不在上下文，无需列。

# 版本库 / 工具 / IDE（不进镜像）
.git
.gitignore
.codebuddy
.idea
.vscode

# Python 缓存与构建产物（不进镜像）
.venv
**/__pycache__
**/*.pyc
**/*.pyo
**/.pytest_cache
**/.ruff_cache
dist
*.egg-info

# 文档子目录（backend 上下文内如有需排除的文档，列于此；当前 common 包无 docs 子目录）

# 注意：
# - frontend/、docs/、logs/、output/、test/ 不在 backend 上下文内，无需忽略。
# - run.bat / run_backend.bat / scripts/ 是启动与部署必需，保留进上下文。
